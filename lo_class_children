assume a class Fred with ListOfFreds all standard

assume another class has these as child elements:

------------------
name: fred
type: lo_element
element: Fred
XML Name: 

would expect API: 

ListOfFreds* getListOfFreds();
Fred * getFred(unsigned int); 
Fred* createFred(); 
getNumFreds();

would expect XML:
 
<listOfFreds><fred/></listOfFreds>

------------------
name: george
type: lo_element
element: Fred
XML Name:

would expect API: 

ListOfFreds* getListOfGeorges();
Fred * getGeorge(unsigned int); 
Fred* createGeorge(); 
getNumGeorges();

would expect XML:
 
<listOfFreds><fred/></listOfFreds>

IMPOSSIBLE AS READ COULD NOT DISTINGUISH
------------------
name: george
type: lo_element
element: Fred
XML Name: element=george

would expect API: 

ListOfFreds* getListOfGeorges();
Fred * getGeorge(unsigned int); 
Fred* createGeorge(); 
getNumGeorges();

would expect XML:
 
<listOfFreds><george/></listOfFreds>

------------------
name: fred
type: lo_element
element: Fred
XML Name: listOfMikes

would expect API: 

ListOfFreds* getListOfMikes();
Fred * getFred(unsigned int); 
Fred* createFred(); 
getNumFreds();

would expect XML:
 
<listOfMikes><fred/></listOfMikes>

-------------------
name: peter
type: lo_element
element: Fred
XML Name: listOfMikes

would expect API: 

ListOfFreds* getListOfMikes();
Fred * getPeter(unsigned int); 
Fred* createPeter(); 
getNumPeters();

would expect XML:
 
<listOfMikes><fred/></listOfMikes>

-------------------
name: peter
type: lo_element
element: Fred
XML Name: element=peter; list=listOfMikes

would expect API: 

ListOfFreds* getListOfMikes();
Fred * getPeter(unsigned int); 
Fred* createPeter(); 
getNumPeters();

would expect XML:
 
<listOfMikes><peter/></listOfMikes>




